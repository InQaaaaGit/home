# Интеграция плагина доступности в тему Moodle

## 🎯 Готовый JavaScript файл для подключения в настройках темы

### 📁 Файл: `theme-integration.js`

Этот файл создан специально для интеграции в настройки темы Moodle. Он добавляет кнопку доступности в существующий интерфейс.

## 🔧 Как подключить в настройках темы:

### 1. Загрузите файл на сервер
Скопируйте файл `theme-integration.js` в папку темы:
```
/path/to/moodle/theme/yourtheme/js/theme-integration.js
```

### 2. Подключите в настройках темы
В настройках темы добавьте:

**CSS:**
```html
<link rel="stylesheet" href="/local/cdo_visuallyimpaired/plugins/dist/css/bvi.min.css">
```

**JavaScript:**
```html
<script src="/local/cdo_visuallyimpaired/plugins/dist/js/bvi.min.js"></script>
<script src="/theme/yourtheme/js/theme-integration.js"></script>
```

### 3. Альтернативный способ через настройки темы
В админке Moodle:
1. Перейдите в **Администрирование → Внешний вид → Темы → Настройки темы**
2. Выберите вашу тему
3. В разделе **Дополнительный HTML** добавьте:

**В раздел "Дополнительный HTML для <head>":**
```html
<link rel="stylesheet" href="/local/cdo_visuallyimpaired/plugins/dist/css/bvi.min.css">
```

**В раздел "Дополнительный HTML для закрывающего тега </body>":**
```html
<script src="/local/cdo_visuallyimpaired/plugins/dist/js/bvi.min.js"></script>
<script src="/theme/yourtheme/js/theme-integration.js"></script>
```

## 🎨 Что делает файл:

### 1. Добавляет кнопку в интерфейс
- Ищет контейнер `#usernavigation`
- Создает кнопку в стиле Moodle
- Вставляет кнопку сразу после `#usernavigation`

### 2. Стилизация кнопки
- Использует иконку FontAwesome `fa-eye`
- Следует стилям Moodle
- Адаптируется под тему

### 3. Инициализация BVI
- Ждет загрузки BVI плагина
- Инициализирует с настройками по умолчанию
- Показывает сообщение об ошибке при проблемах

## 📋 Структура создаваемой кнопки:

```html
<div id="accessibility-button-container" class="d-flex align-items-stretch" style="margin-right: 10px;">
    <div class="popover-region collapsed">
        <a href="#" class="nav-link popover-region-toggle position-relative icon-no-margin bvi-open" 
           role="button" aria-label="Доступность для слабовидящих" title="Доступность для слабовидящих">
            <i class="icon fa fa-eye fa-fw" title="Доступность" role="img" aria-label="Доступность"></i>
        </a>
    </div>
</div>
```

## ⚠️ Важные моменты:

1. **Класс `bvi-open`** обязателен для работы кнопки
2. **Задержка 500мс** для полной загрузки интерфейса Moodle
3. **Проверка существования** - кнопка не добавляется повторно
4. **Обработка ошибок** - graceful degradation при проблемах

## 🔍 Отладка:

### Включите консоль браузера (F12) и ищите сообщения:
- ✅ "Кнопка доступности добавлена в интерфейс Moodle"
- ✅ "BVI плагин успешно инициализирован в теме"
- ❌ "Контейнер #usernavigation не найден"
- ❌ "BVI плагин не загрузился в течение 10 секунд"

## 🎯 Результат:

После подключения:
- ✅ Кнопка доступности появляется в интерфейсе Moodle
- ✅ Кнопка стилизована под тему
- ✅ При нажатии открывается панель настроек доступности
- ✅ Все настройки применяются корректно

## 📱 Адаптивность:

Кнопка автоматически адаптируется под:
- Различные темы Moodle
- Мобильные устройства
- Разные размеры экрана

## 🔧 Настройка:

Вы можете изменить:
- **Иконку:** замените `fa-eye` на другую иконку FontAwesome
- **Позицию:** измените `margin-right` для позиционирования
- **Стили:** добавьте CSS для кастомизации внешнего вида

## 💡 Преимущества:

- ✅ Интеграция в существующий интерфейс
- ✅ Следование стилям Moodle
- ✅ Автоматическая адаптация под тему
- ✅ Простое подключение через настройки темы
- ✅ Обработка ошибок и graceful degradation

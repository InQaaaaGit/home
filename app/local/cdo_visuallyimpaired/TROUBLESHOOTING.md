# Устранение проблем с плагином доступности

## ✅ Проблема решена: "No define call for local_cdo_visuallyimpaired/amd"

### Что было исправлено:

1. **Убраны AMD модули** - перешли на простые JavaScript файлы без AMD
2. **Прямое подключение** - BVI плагин загружается через script tag
3. **Упрощенная архитектура** - убрана сложная система модулей

### Новые файлы:
- `init-simple.js` - простая инициализация без AMD
- `debug-simple.js` - простая отладка без AMD
- `lib.php` - обновлен для прямого подключения JS

## 🔧 Как проверить работу плагина:

### 1. Включите режим разработки
```
Администрирование → Разработка → Отладка → Режим разработки = Включен
```

### 2. Откройте консоль браузера (F12)
Ищите сообщения:
- ✅ "CDO Visually Impaired Plugin: Инициализация отладки"
- ✅ "BVI плагин успешно инициализирован"
- ✅ "Плавающая кнопка доступности добавлена"

### 3. Проверьте кнопку доступности
- В правом верхнем углу должна появиться синяя кнопка "Доступность" 👁️
- При нажатии должна открыться панель настроек

## 🚨 Если проблемы продолжаются:

### Ошибка: "No define call for..."
**Решение:** Очистите кэш браузера (Ctrl+F5) и кэш Moodle

### Кнопка не появляется
1. Проверьте консоль на ошибки JavaScript
2. Убедитесь, что плагин установлен корректно
3. Проверьте, что не на странице администрирования

### BVI плагин не загружается
1. Проверьте, что файл `bvi.min.js` доступен
2. Убедитесь, что нет блокировки скриптов
3. Проверьте права доступа к файлам

## 📋 Проверочный список:

- [ ] Плагин установлен в `/local/cdo_visuallyimpaired/`
- [ ] Все файлы на месте (js/, css/, img/, lang/)
- [ ] Кэш Moodle очищен
- [ ] Кэш браузера очищен
- [ ] JavaScript включен в браузере
- [ ] Режим разработки включен (для отладки)

## 🔍 Отладка:

### Включите подробную отладку:
1. Включите режим разработки в Moodle
2. Откройте консоль браузера (F12)
3. Обновите страницу
4. Ищите сообщения от "CDO Visually Impaired"

### Проверьте загрузку файлов:
- CSS: `/local/cdo_visuallyimpaired/css/bvi.min.css`
- JS: `/local/cdo_visuallyimpaired/js/bvi.min.js`
- Init: `/local/cdo_visuallyimpaired/js/init-simple.js`

## 📞 Поддержка:

Если проблема не решается, предоставьте:
- Версию Moodle
- Версию браузера
- Логи из консоли браузера
- Список установленных плагинов
- Скриншоты ошибок

## 🎯 Ожидаемый результат:

После исправлений плагин должен:
- ✅ Загружаться без ошибок AMD
- ✅ Показывать кнопку "Доступность" в правом верхнем углу
- ✅ Открывать панель настроек при нажатии
- ✅ Применять настройки доступности
- ✅ Работать стабильно на всех страницах

## 🔄 Архитектура плагина:

```
lib.php
├── before_http_headers() → CSS
└── before_footer() → JS файлы
    ├── bvi.min.js (BVI плагин)
    ├── init-simple.js (инициализация)
    └── debug-simple.js (отладка, только в режиме разработки)
```

## 💡 Преимущества нового подхода:

- ✅ Нет конфликтов с AMD/RequireJS
- ✅ Простая и понятная архитектура
- ✅ Быстрая загрузка
- ✅ Совместимость со всеми версиями Moodle
- ✅ Легкая отладка

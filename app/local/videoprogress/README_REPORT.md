# Отчет по прогрессу видео - local_videoprogress

## Описание

Плагин `local_videoprogress` предоставляет функциональность для отслеживания и отображения прогресса просмотра видео учениками в курсах Moodle. Основная функция - создание отчета, который показывает в табличном виде прогресс каждого ученика по каждому видео.

## Структура отчета

Отчет представляет собой таблицу, где:
- **Строки** - ФИО учеников (студентов)
- **Столбцы** - видео, которые они посмотрели
- **Ячейки** - процент просмотра (0-100%) и дата последнего обновления

## Функциональность

### Основные возможности:
1. **Просмотр прогресса** - детальная таблица с прогрессом всех учеников по всем видео в курсе
2. **Цветовая индикация** - ячейки окрашены в зависимости от процента просмотра:
   - 🟢 Отличный (90-100%) - зеленый
   - 🔵 Хороший (70-89%) - голубой  
   - 🟡 Средний (40-69%) - желтый
   - 🔴 Плохой (1-39%) - красный
   - ⚪ Не просмотрено (0%) - серый
3. **Экспорт данных** - возможность скачать отчет в форматах CSV и Excel
4. **Фильтрация по курсам** - выбор конкретного курса для просмотра
5. **Адаптивный дизайн** - таблица адаптируется под размер экрана

### Файлы отчета:

#### Основные файлы:
- `report.php` - главная страница отчета
- `classes/report_manager.php` - класс для получения и обработки данных
- `classes/output/renderer.php` - рендерер для отображения отчета
- `settings.php` - настройки и навигация в админ-панели

#### Языковые файлы:
- `lang/ru/local_videoprogress.php` - русская локализация
- `lang/en/local_videoprogress.php` - английская локализация

## Доступ к отчету

### Через админ-панель:
1. Войти как администратор
2. Перейти в "Администрирование сайта"
3. Выбрать "Плагины" → "Локальные плагины" → "Прогресс видео"
4. Нажать "Отчет по прогрессу видео"

### Прямая ссылка:
```
http://your-moodle-site.com/local/videoprogress/report.php
```

## Права доступа

Для работы с отчетом настроены следующие права:

1. **`local/videoprogress:view`** - Просмотр прогресса видео
   - Доступно: преподавателям и администраторам
   - Контекст: курс

2. **`local/videoprogress:manage`** - Управление отчетами
   - Доступно: только администраторам  
   - Контекст: система

## Использование

### 1. Выбор курса
- На странице отчета выберите курс из выпадающего списка
- Отображаются только курсы, в которых есть данные о прогрессе видео

### 2. Просмотр отчета  
- После выбора курса автоматически загружается таблица с данными
- Столбец с именами студентов закреплен слева для удобства навигации
- При наведении на строку она подсвечивается

### 3. Экспорт данных
- Кнопка "Экспорт в CSV" - для работы с данными в Excel/Calc
- Кнопка "Экспорт в Excel" - для создания .xlsx файла

## Структура данных

### Таблица `local_videoprogress`:
```sql
- id (int) - уникальный идентификатор
- userid (int) - ID пользователя
- cmid (int) - ID модуля курса
- videoid (varchar) - идентификатор видео
- progress (float) - процент просмотра (0-100)
- segments (text) - JSON с просмотренными сегментами
- duration (float) - длительность видео в секундах
- timecreated (int) - время создания записи
- timemodified (int) - время последнего обновления
```

### Пример данных отчета:
```php
$report_data = [
    'users' => [
        123 => (object)[
            'id' => 123,
            'firstname' => 'Иван',
            'lastname' => 'Петров', 
            'email' => 'ivan@example.com'
        ]
    ],
    'videos' => [
        'video1' => [
            'id' => 'video1',
            'name' => 'Введение в курс',
            'cmid' => 456
        ]
    ],
    'progress' => [
        123 => [
            'video1' => [
                'progress' => 75.5,
                'timemodified' => 1647895200
            ]
        ]
    ]
];
```

## API для разработчиков

### Основные методы класса `report_manager`:

```php
// Получить данные отчета для курса
$report_data = $report_manager->get_report_data($courseid);

// Получить список доступных курсов
$courses = $report_manager->get_available_courses();

// Скачать отчет в CSV
$report_manager->download_report($courseid, 'csv');

// Скачать отчет в Excel  
$report_manager->download_report($courseid, 'excel');
```

### Основные методы класса `video_progress`:

```php
// Обновить прогресс
video_progress::update_progress($userid, $cmid, $videoid, $progress);

// Получить прогресс
$progress = video_progress::get_progress($userid, $cmid, $videoid);

// Обновить с сегментами
video_progress::update_segments($userid, $cmid, $videoid, $segments, $duration);
```

## Настройки

В административной панели доступны следующие настройки:

1. **Интервал отслеживания** - как часто отправлять данные на сервер (по умолчанию 5 секунд)
2. **Включение отслеживания** - общий переключатель функциональности

## Техническая информация

### Требования:
- Moodle 4.5+
- PHP 8.1+
- Права администратора для доступа к отчету

### Архитектура:
- Следует принципам SOLID, KISS, DRY
- Использует стандартные API Moodle для работы с БД, рендерингом и правами
- Поддерживает локализацию
- Адаптивный дизайн с использованием Bootstrap

### Производительность:
- Оптимизированные SQL-запросы с JOIN для минимизации количества обращений к БД
- Кэширование данных на уровне сессии
- Ленивая загрузка данных для больших курсов

## Примеры использования

Подробные примеры использования API и интеграции с другими плагинами смотрите в файле `example_usage.php`.

## Поддержка

При возникновении вопросов или проблем:
1. Проверьте права доступа пользователя
2. Убедитесь, что в курсе есть данные о прогрессе видео
3. Проверьте логи Moodle на наличие ошибок 
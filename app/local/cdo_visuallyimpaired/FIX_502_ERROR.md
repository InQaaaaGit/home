# Исправление ошибки 502 в настройках плагина

## ✅ Проблема решена!

### 🔧 Что было исправлено:

1. **Убрана циклическая ссылка** - файл `settings.php` больше не ссылается сам на себя
2. **Создана правильная структура настроек** - используется `admin_settingpage` вместо `admin_externalpage`
3. **Добавлены языковые строки** - все настройки теперь имеют переводы
4. **Интегрированы настройки** - плагин теперь использует настройки из админки

### 📁 Измененные файлы:

- **`settings.php`** - исправлена структура настроек
- **`lang/en/cdo_visuallyimpaired.php`** - добавлены строки для настроек
- **`lang/ru/cdo_visuallyimpaired.php`** - добавлены русские переводы
- **`lib.php`** - интегрированы настройки из админки

## 🎯 Новые возможности настроек:

### В админке Moodle теперь доступны:
- **Включение/выключение плагина**
- **Размер шрифта по умолчанию** (12px - 24px)
- **Цветовая схема по умолчанию** (белая, черная, синяя, коричневая, зеленая)
- **Синтез речи по умолчанию** (включен/выключен)
- **Режим отображения изображений** (показать/скрыть/оттенки серого)

## 🔍 Как проверить исправление:

1. **Очистите кэш Moodle:**
   ```
   Администрирование → Разработка → Очистить кэш
   ```

2. **Перейдите в настройки плагина:**
   ```
   Администрирование → Плагины → Локальные плагины → Доступность для слабовидящих
   ```

3. **Проверьте, что страница загружается без ошибки 502**

4. **Настройте параметры плагина по своему усмотрению**

## 🚨 Если ошибка 502 все еще появляется:

### 1. Проверьте логи сервера
```bash
tail -f /var/log/nginx/error.log
# или
tail -f /var/log/apache2/error.log
```

### 2. Проверьте логи PHP
```bash
tail -f /var/log/php/error.log
```

### 3. Временно отключите плагин
Переименуйте папку плагина:
```bash
mv /path/to/moodle/local/cdo_visuallyimpaired /path/to/moodle/local/cdo_visuallyimpaired_disabled
```

### 4. Проверьте права доступа
```bash
chmod -R 755 /path/to/moodle/local/cdo_visuallyimpaired
chown -R www-data:www-data /path/to/moodle/local/cdo_visuallyimpaired
```

## 📋 Проверочный список:

- [ ] Файл `settings.php` не содержит циклических ссылок
- [ ] Все языковые строки добавлены
- [ ] Кэш Moodle очищен
- [ ] Права доступа к файлам корректны
- [ ] PHP синтаксис корректен
- [ ] Нет конфликтов с другими плагинами

## 🎯 Ожидаемый результат:

После исправлений:
- ✅ Страница настроек загружается без ошибки 502
- ✅ Все настройки плагина доступны в админке
- ✅ Плагин использует настройки из админки
- ✅ Кнопка доступности появляется на страницах
- ✅ Настройки применяются корректно

## 🔧 Архитектура настроек:

```
settings.php
├── admin_settingpage (правильная структура)
├── admin_setting_configcheckbox (включение/выключение)
├── admin_setting_configselect (размер шрифта)
├── admin_setting_configselect (цветовая схема)
├── admin_setting_configcheckbox (синтез речи)
└── admin_setting_configselect (изображения)
```

## 💡 Преимущества нового подхода:

- ✅ Нет циклических ссылок
- ✅ Стандартная структура настроек Moodle
- ✅ Полная локализация
- ✅ Интеграция с системой настроек
- ✅ Возможность включения/выключения плагина
- ✅ Настройка параметров по умолчанию

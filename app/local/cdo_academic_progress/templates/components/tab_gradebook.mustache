<ul class="nav nav-tabs" id="academic_progress_gradebook" role="tablist">
    {{#progress}}
        <li class="nav-item" role="presentation">
            <a
                    class="nav-link {{active_gb}}"
                    id="{{order}}-gradebook"
                    data-toggle="tab"
                    href="#nav-gradebook-{{order}}" role="tab"
                    aria-controls="{{order}}"
                    aria-selected="true">
                {{#str}}gradebook, local_cdo_academic_progress{{/str}} {{gradebook}}
            </a>
        </li>
    {{/progress}}
</ul>
<div class="tab-content" id="nav_tab_content_academic_progress_gradebook1">
    {{#progress}}

        <div class="tab-pane fade {{show_gb}} {{active_gb}}" id="nav-gradebook-{{order}}" role="tabpanel"
             aria-labelledby="ap1{{order}}">
            {{^attestation_not_empty}}
                <div class="alert alert-warning mt-2" role="alert">
                    <i class="fa fa-exclamation-triangle" aria-hidden="true"></i>
                    <strong>Успеваемость не найдена</strong>
                </div>
            {{/attestation_not_empty}}
            {{#attestation_not_empty}}
                {{> local_cdo_academic_progress/components/tab_academic_progress}}
            {{/attestation_not_empty}}
        </div>
    {{/progress}}
</div>
